#include <iostream>
#include <vector>
using namespace std;

/*using vi = vector<int>;
using vvi = vector<vi>;
vvi adjList; int V, E;
const int MAX = 1E9;
bool visited[MAX];*/


int backTrack(int nodoActual, int viajes)
{
	
}

int main()
{
   
	int ciudades, inicio, numFin, viajes;
	cin >> ciudades>> inicio>> numFin>> viajes;
	while (ciudades != 0 && inicio != 0 && numFin != 0 && viajes != 0)
	{

	}
}